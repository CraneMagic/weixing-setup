# Weixing Setup 更新日志

## [2025-09-28] - 架构优化：统一数据目录结构

### 🔧 主要变更

#### Docker Compose 配置更新

- **移除独立 error 目录挂载**：所有 TCP 服务不再单独挂载 `./error:/usr/src/app/error:rw`
- **统一数据管理**：现在所有数据都通过 `./data:/usr/src/app/data:rw` 统一管理
- **简化配置**：减少了 Docker 挂载点数量，降低了配置复杂度

#### 架构改进

- **数据统一性**：错误图片现在存储在 `data/error/` 目录下
- **管理便利性**：所有业务数据都在 `data/` 目录下，便于备份、迁移、清理
- **运维友好**：简化了存储管理，统一的数据目录结构

### 📁 新的目录结构

```
weixing-setup/
├── data/                    # 统一数据目录
│   ├── images/              # 正常图片（由服务自动创建）
│   └── error/               # 错误图片（由服务自动创建）
├── logs/                    # 日志目录
├── temp/                    # 临时文件目录
└── docker-compose.yml       # 更新的Docker配置
```

### 🔄 向后兼容性

- **API 接口**：保持不变
- **数据库字段**：格式保持一致
- **服务功能**：完全兼容
- **部署方式**：无需额外配置

### 📝 部署说明

#### 首次部署

1. 确保 `data/` 目录存在且有写权限
2. 服务会自动创建 `data/images/` 和 `data/error/` 子目录
3. 所有图片数据都会存储在 `data/` 目录下

#### 从旧版本迁移

1. 如果之前有独立的 `error/` 目录，可以手动迁移到 `data/error/`
2. 或者直接删除旧的 `error/` 目录，让服务重新创建

### ⚠️ 注意事项

- 确保 `data/` 目录有足够的存储空间
- 备份时只需要备份 `data/` 目录即可
- 清理策略可以统一针对 `data/` 目录制定

### 🎯 优势

1. **简化管理**：统一的数据目录结构
2. **降低复杂度**：减少 Docker 挂载点
3. **便于运维**：统一的备份和清理策略
4. **架构清晰**：符合数据分层设计原则

---

## 历史版本

### [之前版本]

- 使用独立的 `error/` 目录存储错误图片
- TCP 服务单独挂载 error 目录
- 数据分散在多个目录中
