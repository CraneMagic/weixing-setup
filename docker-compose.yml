# 内存配额按「单机约 12GB RAM」收敛：各服务 limits 合计约 ~8.8GB，留出头给系统与 Docker。
services:
  postgres:
    image: registry.yijunstudio.xyz/mirror/postgres:15
    restart: always
    container_name: postgres
    hostname: postgres
    ports:
      - "5432:5432"
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
    shm_size: 512m
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    networks:
      - weixing-network
    deploy:
      resources:
        reservations:
          cpus: "0.25"
          memory: 128M
        limits:
          cpus: "0.50"
          memory: 768M
  websocket-server:
    image: registry.yijunstudio.xyz/weixing-websocket-server:${TAG:-latest}
    container_name: websocket-server
    hostname: websocket-server
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-8080}:${WS_PORT:-8080}"
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
    user: "${UID}:${GID}"
    volumes:
      - ./logs:/app/logs:rw
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    # depends_on:
    #   - backend-service
    networks:
      - weixing-network
    # 12GB 主机：给到 1.75G，大帧缓冲更宽裕
    deploy:
      resources:
        reservations:
          cpus: "0.20"
          memory: 384M
        limits:
          cpus: "0.60"
          memory: 1792M
  udp-server:
    image: registry.yijunstudio.xyz/weixing-udp-server:${TAG:-latest}
    container_name: udp-server
    hostname: udp-server
    restart: unless-stopped
    ports:
      - "2000:2000/udp"
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
      WEIXING_SERVICE_BASE_URL: http://backend-service:3000
      ECC_MODEL_PATH: ${ECC_MODEL_PATH:-}
    user: "${UID}:${GID}"
    volumes:
      - ./data:/usr/src/app/data:rw
      - ./logs:/usr/src/app/logs:rw
      # 偏心补偿模型：放置 ecc_model.json 后取消注释即可覆盖镜像内默认模型
      # - ./ecc_model.json:/usr/src/app/ecc_model.json:ro
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    # depends_on:
    #   - backend-service
    networks:
      - weixing-network
    deploy:
      resources:
        reservations:
          cpus: "0.25"
          memory: 128M
        limits:
          cpus: "0.25"
          memory: 384M
  # tcp-server:
  #   image: registry.yijunstudio.xyz/weixing-tcp-server:${TAG:-latest}
  #   container_name: tcp-server
  #   hostname: tcp-server
  #   restart: unless-stopped
  #   ports:
  #     - "8999:8999"
  #   env_file:
  #     - .env
  #   user: "${UID}:${GID}"
  #   volumes:
  #     - ./data:/usr/src/app/data:rw
  #     - ./logs:/usr/src/app/logs:rw
  #     - ./error:/usr/src/app/error:rw
  #     - ./temp:/usr/src/app/temp:rw
  #   depends_on:
  #     - backend-service
  #     - websocket-server
  #   networks:
  #     - weixing-network
  tcp-server-1:
    image: registry.yijunstudio.xyz/weixing-tcp-server:${TAG:-latest}
    container_name: tcp-server-1
    hostname: tcp-server-1
    restart: unless-stopped
    ports:
      - "8999:8999"
    env_file:
      - .env
    environment:
      - CAMERA_ID=1
      - CAMERA_PORT=8999
      - CAMERA_NAME=camera1
      - BUFFER_SIZE=65536
      - THREAD_POOL_SIZE=4
      - TZ=Asia/Shanghai
    user: "${UID}:${GID}"
    volumes:
      - ./data:/usr/src/app/data:rw
      - ./logs:/usr/src/app/logs:rw
      - ./temp:/usr/src/app/temp:rw
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    depends_on:
      - backend-service
      - websocket-server
    networks:
      - weixing-network
    deploy:
      resources:
        reservations:
          cpus: "0.30"
          memory: 128M
        limits:
          cpus: "0.60"
          memory: 512M
  tcp-server-2:
    image: registry.yijunstudio.xyz/weixing-tcp-server:${TAG:-latest}
    container_name: tcp-server-2
    hostname: tcp-server-2
    restart: unless-stopped
    ports:
      - "9000:8999"
    env_file:
      - .env
    environment:
      - CAMERA_ID=2
      - CAMERA_PORT=8999
      - CAMERA_NAME=camera2
      - BUFFER_SIZE=65536
      - THREAD_POOL_SIZE=4
      - TZ=Asia/Shanghai
    user: "${UID}:${GID}"
    volumes:
      - ./data:/usr/src/app/data:rw
      - ./logs:/usr/src/app/logs:rw
      - ./temp:/usr/src/app/temp:rw
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    depends_on:
      - backend-service
      - websocket-server
    networks:
      - weixing-network
    deploy:
      resources:
        reservations:
          cpus: "0.30"
          memory: 128M
        limits:
          cpus: "0.60"
          memory: 512M
  tcp-server-3:
    image: registry.yijunstudio.xyz/weixing-tcp-server:${TAG:-latest}
    container_name: tcp-server-3
    hostname: tcp-server-3
    restart: unless-stopped
    ports:
      - "9001:8999"
    env_file:
      - .env
    environment:
      - CAMERA_ID=3
      - CAMERA_PORT=8999
      - CAMERA_NAME=camera3
      - BUFFER_SIZE=65536
      - THREAD_POOL_SIZE=4
      - TZ=Asia/Shanghai
    user: "${UID}:${GID}"
    volumes:
      - ./data:/usr/src/app/data:rw
      - ./logs:/usr/src/app/logs:rw
      - ./temp:/usr/src/app/temp:rw
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    depends_on:
      - backend-service
      - websocket-server
    networks:
      - weixing-network
    deploy:
      resources:
        reservations:
          cpus: "0.30"
          memory: 128M
        limits:
          cpus: "0.60"
          memory: 512M
  backend-service:
    image: registry.yijunstudio.xyz/weixing-service:${TAG:-latest}
    container_name: backend-service
    hostname: backend-service
    user: "${UID}:${GID}"
    group_add:
      - dialout
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      TZ: Asia/Shanghai
      CV_SERVICE_URL: http://weixing-cv:3001
    volumes:
      - ./data:/app/data:rw
      - ./logs:/app/logs:rw
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"
      - "/dev/ttyUSB1:/dev/ttyUSB1"
    privileged: true
    env_file:
      - .env
    depends_on:
      - postgres
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://backend-service:3000/api/parameters",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - weixing-network
    deploy:
      resources:
        reservations:
          cpus: "0.40"
          memory: 512M
        limits:
          cpus: "1.00"
          memory: 1792M
  frontend:
    image: registry.yijunstudio.xyz/weixing-frontend:${TAG:-latest}
    container_name: frontend
    hostname: frontend
    restart: unless-stopped
    ports:
      - "3002:3000"
    env_file:
      - .env
    environment:
      TZ: Asia/Shanghai
    user: "${UID}:${GID}"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    # depends_on:
    #   - backend-service
    networks:
      - weixing-network
    deploy:
      resources:
        reservations:
          cpus: "0.15"
          memory: 128M
        limits:
          cpus: "0.50"
          memory: 512M
  weixing-cv:
    image: registry.yijunstudio.xyz/weixing-cv:${TAG:-latest}
    container_name: weixing-cv
    hostname: weixing-cv
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    user: "${UID}:${GID}"
    volumes:
      - ./data/images:/app/data/images:rw
      - ./output:/app/output:rw
      - ./logs:/app/logs:rw
      - ./baseline.mat:/app/baseline.mat:ro
    environment:
      - BASELINE_PATH=/app/baseline.mat
      - CALIBRATION_SERVER_PORT=3001
      - IMAGE_DIRECTORY=/app/data/images
      - IMAGE_SOURCE_MODE=docker_volume
      - CALIBRATION_MODE=auto
      - BACKEND_API_URL=http://backend-service:3000
      - WS_URL=ws://websocket-server:8080
      - TZ=Asia/Shanghai
    depends_on:
      - websocket-server
      - backend-service
    networks:
      - weixing-network
    # 12GB 主机：shm 增到 512M，CV 内存上限提到 2G
    shm_size: 512m
    # 日志配置，防止日志文件过大
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    # 健康检查
    healthcheck:
      # 真正探标定服务的 3001 端口：进程死掉时 docker ps 会显示 unhealthy
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:3001/health', timeout=5)",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        reservations:
          cpus: "0.50"
          memory: 512M
        limits:
          cpus: "2.00"
          memory: 2048M

networks:
  weixing-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
